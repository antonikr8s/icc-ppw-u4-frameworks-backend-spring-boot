server:
  servlet:
    context-path: /api

spring:
  application:
    name: server_back
  profiles:
    active: dev

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: when-authorized